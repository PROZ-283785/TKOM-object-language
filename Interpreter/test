funkcja(a in, b out, c in out, d){


    d = a + -b;
    a = b * c.b;

    when( i == 1 ){
        out<<'1';
    } else when( i == 2 ){
        in>>a;
    } else {
        this.a = b;
    }

    loop( i = 1; i < 5; i = i + 1){
        a = b;
    }

    fun(c out);
    fun(d out);
    a = c + d + a + 2 + a.b;
    a = car.run(a in out);
    loop(a:b){
        in>>a;
    }
    out<< a + b + c;
}

obiekt{

    operator = (){

    }
    fun(a in, b out){

    }

}

obiekt extends obiekt1{

}