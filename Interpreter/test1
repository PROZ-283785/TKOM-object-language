x = 'text';

#comment
Car extends Vehicle{
    maxSpeed {get; set;} = 10;
    age = 100;
}
#comment

changeSpeed(newSpeed) extends Car{
    maxSpeed.() == newSpeed;
}

kwadrat(liczba, wynik out){
    wynik = liczba * liczba - liczba + liczba / liczba;
}

main(){
    in >> wartosc;
    wynik = none;
    kwadrat(5, wynik);
    out >> wynik;
    x = 'asdfdasf';
}

#comment
