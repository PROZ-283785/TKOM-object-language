Car extends Vehicle{
    maxSpeed {get; set;} = 10;
    age = 100;
}


changeSpeed(newSpeed) extends Car{
    maxSpeed.() == newSpeed;
}

kwadrat(liczba, wynik out){
    wynik = liczba * liczba - liczba + liczba / liczba;
}

main(){
    in >> wartosc;
    wynik = none;
    kwadrat(5, wynik);
    out >> wynik;

}

#comment
