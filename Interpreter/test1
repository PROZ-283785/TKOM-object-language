object Car(Vehicle){
    maxSpeed {get; set;} = 10;
    age = 100;
}


fun changeSpeed(newSpeed){
    maxSpeed.() == newSpeed;
}

fun kwadrat(liczba, wynik out){
    wynik = liczba * liczba - liczba + liczba / liczba;
}

fun main(){
    in>> wartosc;
    wynik = none;
    kwadrat(wartosc, wynik);
    out>> wynik;

}
